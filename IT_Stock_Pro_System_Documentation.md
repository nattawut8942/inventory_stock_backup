# System Documentation: IT Stock Pro

## 1. บทนำและภาพรวมระบบ (System Overview)
**IT Stock Pro** คือระบบจัดการสินค้าคงคลังบนเทคโนโลยี Web Application (Client-Server Architecture) ซึ่งพัฒนาขึ้นมาเพื่อแก้ไขปัญหาการจัดการคลังอุปกรณ์ไอทีที่เดิมใช้งานบนเอกสารกระดาษและ Excel โดยเพิ่มประสิทธิภาพในการตรวจสอบสินค้า การรับเข้าแบบเรียลไทม์ และลดข้อผิดพลาดในการคำนวณ 

**จุดเด่นที่แตกต่างจากระบบเดิม:**
1.  **Real-time Stock Update:** จำนวนสินค้าคงเหลือจะอัปเดตแบบเรียลไทม์ทันทีที่มีการรับเข้า (Receive) หรือเบิกออก (Dispense)
2.  **Minimum Stock Alert:** รองรับการตั้งค่าจุดสั่งซื้อขั้นต่ำ (Min Stock) พร้อมแจ้งเตือนเป็นสีแดง (Alert) และส่ง Email แจ้งเตือนฝ่ายจัดซื้อเพื่อป้องกันภาวะของขาดสต็อก
3.  **Barcode & AI Integration:** รองรับการใช้บาร์โค้ดไร้สายในการเบิก/รับของ แทนการจดด้วยมือ และมี AI OCR (Tesseract.js) ในการอ่านบิลเพื่อลดภาระการพิมพ์
4.  **MA & License Monitoring:** แจ้งเตือนล่วงหน้าเมื่อลิขสิทธิ์ซอฟต์แวร์หรือประกัน MA ใกล้หมดอายุ

---

## 2. โครงสร้างเทคโนโลยีและไลบรารีที่ใช้งาน (Tech Stack & Libraries)

การพัฒนาระบบนี้แบ่งการทำงานออกเป็น 2 ส่วนหลัก เพื่อให้โครงสร้างของซอร์สโค้ดไม่ผูกติดกัน (Decoupled System) ซึ่งจะง่ายต่อการเพิ่มฟีเจอร์ใหม่

### 2.1. Frontend (ฝั่งผู้ใช้งาน)
ส่วนต่อประสานกับผู้ใช้ (User Interface) ทำหน้าที่รับคำสั่งจากผู้ใช้งาน ส่งไปประมวลผลเซิร์ฟเวอร์ และแสดงผลเป็นหน้าจอที่สวยงาม ใช้งานง่าย
*   **เฟรมเวิร์คหลัก:** React 19 และ Vite (Build Tool) ช่วยให้แอปพลิเคชันทำงานลื่นไหลแบบ Single Page Application (โหลดหน้าครั้งเดียว ไม่ต้องรีเฟรชบ่อยๆ)
*   **การจัดการสไตล์และ Design:** Tailwind CSS & Framer Motion ใช้สำหรับ UI Layout ขั้นสูง จัดสี แจ้งเตือนแบบ Toast รวมถึงสร้าง Effect ให้หน้าน่าสนใจ
*   **การเชื่อมต่อข้อมูล API:** Axios
*   **การรายงานผล (Data Visualization):** Recharts สร้างกราฟแท่งหรือ Pie Chart สรุปทิศทางบนหน้า Dashboard
*   **AI OCR (ส่วนประมวลผลอักขระจากภาพ):** Tesseract.js สำหรับสแกนอ่านข้อความ และ pdfjs-dist สำหรับแปลงไฟล์ PDF บิลเป็นรูปภาพ
*   **เครื่องมืออื่นๆ:** React-Barcode สำหรับออกบาร์โค้ดสติกเกอร์

### 2.2. Backend (ฝั่งประมวลผลเซิร์ฟเวอร์)
เป็นส่วนควบคุมการทำงานและเชื่อมต่อฐานข้อมูล เปรียบเสมือนศูนย์กลางตรรกะทั้งหมด
*   **เซิร์ฟเวอร์หลัก:** Node.js พร้อมระบบ Express.js Framework ในการเปิดรับ API Requests แบบข้ามโดเมน (CORS) 
*   **การติดต่อฐานข้อมูล:** mssql (Microsoft SQL Server Driver) ควบคุมการใช้งานฐานข้อมูลแบบ Relational Model ช่วยให้คิวรียอดคงเหลือได้อย่างแม่นยำ
*   **Job Scheduler:** node-cron สร้างโปรเซสเซอร์ Background ในการทำงานประจำวันอัตโนมัติ (เช่นตรวจอีเมล หรือสรุปจำนวนสินค้าเช้า-เย็น)
*   **Email Notification:** Nodemailer จัดการส่วนระบบส่งกล่องข้อความแบบ SMTP ไปยังอีเมลของ Manager
*   **การดักจับไฟล์:** Multer ไลบรารีอัปโหลดไฟล์/รูปอุปกรณ์หรือใบ Invoice ไปยังคลังรูปใน Server

---

## 3. สถาปัตยกรรมฐานข้อมูล (Database Schema)

โครงสร้างฐานข้อมูลออกแบบบน SQL Server ให้อยู่ในรูปแบบ RDBMS (Relational Database Management System) ไม่เก็บบันทึกยอดคงเหลือสินค้าแบบแก้ไขตัวเลขดิบๆ แต่ใช้หลักการของ **"บัญชีแยกประเภท (Ledger Transactions)"**

**ตารางหลัก (Core Tables):**
1.  **`dbo.Stock_Products`:** ตารางเก็บรายละเอียดอุปกรณ์ (Master Data) ทั้งหมด เช่น ชื่อหมวดหมู่, รูป, Min Stock, บาร์โค้ด และ Location
2.  **`dbo.Stock_Transactions`:** *(ตารางหัวใจของระบบ)* เก็บประวัติการเดินของสต็อกทั้งหมด เมื่อมีการรับของ ระบบจะ Insert ROW ใหม่สถานะ `IN` และเมื่อมีการจ่ายของออกจะ Insert ROW สถานะ `OUT` การคำนวณยอดคงเหลือจะใช้วิธีนำผลรวม IN หักลบด้วย OUT ซึ่งเป็นมาตรฐานเดียวกับบัญชี 
3.  **`dbo.Stock_PurchaseOrders` / `dbo.Stock_PODetails`:** ส่วนของรายการใบสั่งซื้อและของที่บริษัทออเดอร์ (ออเดอร์ใหญ่ 1 ใบประกอบด้วยสินค้าย่อยหลายไอเทม) มีระบบติดตามสถานะว่าผู้ใช้นำของเข้าแล้ว (Completed) หรือของยังมาไม่ถึง (Pending)
4.  **`dbo.Stock_StockCountSessions` / `dbo.Stock_StockCountItems`:** ชุดตารางที่ออกแบบไว้สำหรับ **ระบบตรวจนับสต็อก (Stock Check)** เพื่อจัดเก็บประวัติการเอา Tablet เดินนับของประจำเดือน
5.  **`dbo.Stock_Locations`:** จัดการตำแหน่งที่เก็บของเชิงภูมิศาสตร์หรือตามตู้
6.  **`dbo.Stock_Vendors`:** ลิสต์บริษัทคู่ค้า (ซัพพลายเออร์) เพื่อประโยชน์ในการค้นหายอดสั่งซื้อรายปี 

---

## 4. โครงสร้างโฟลเดอร์ของโปรเจกต์ (Directory Structure)

```text
IT-STOCK-PRO/
├── backend/                       # โฟลเดอร์ที่รันผ่าน Node.js
│   ├── src/
│   │   ├── config/                # ตั้งค่า DB, Email Auth
│   │   ├── controllers/           # ไฟล์คำสั่งทางธุรกิจ เช่น productController.js, transactionController.js
│   │   ├── routes/                # กำหนดเส้นทาง URL (API Endpoints)
│   │   └── utils/                 # ฟังก์ชันเสริม เช่น ระบบ Scheduler, OCR
│   ├── server.js                  # ไฟล์ Entry Point ของเซิร์ฟเวอร์เปิด Port ต้อนรับ
│
└── frontend/                      # โฟลเดอร์หน้าบ้าน (React)
    ├── src/
    │   ├── components/            # UI เล็กๆ ที่นำมาใช้ซ้ำ (อาทิ ปุ่ม, Modal, ฟอร์มสแกนบาร์โค้ด)
    │   ├── pages/                 # หน้าจอแต่ละเมนู เช่น DashboardPage, InventoryPage, ReceivePage
    │   ├── lib/                   # โค้ดเชื่อมต่อ Network API
    │   ├── App.jsx                # กำหนดว่า URL ไหน จะวิ่งไปโชว์ที่ไฟล์เพจไหน
    │   └── index.css              # ใส่โค้ดตั้งค่า Tailwind CSS

```

---

## 5. การวิเคราะห์ความคุ้มค่าและความปลอดภัย (ROI & Security Considerations)

*   **Data Integrity (ความแม่นยำทางข้อมูล):** พัฒนาระบบใช้ตาราง Transactions หมายความว่าผู้ใช้งานระดับล่าง ไม่สามารถป้อนข้อมูลเข้าไปแก้ไขตัวเลขสินค้าตรงๆ ในฐานข้อมูลได้ ทำให้ปิดช่องโหว่พนักงานขโมยของหรือย้ายของโดยที่ระบบไม่รับรู้ ทุกการขยับจะมี Timestamp ติดอยู่เสมอ
*   **Time & Cost Saving (ประหยัดค่าใช้จ่าย):** จากที่ต้องให้พนักงานเดินจดและเสียเวลาคำนวณ Excel เพื่อส่งรายงานเดือนละครั้ง ระบบทำการย่นเวลาด้วยการให้ AI ช่วยคำนวณมูลค่าคงคลัง สรุปสินค้าหมุนเวียน (Fast/Slow moving) เป็นกราฟทันที และเตือนของหมดให้อัตโนมัติ ลดภาวการณ์เสียโอกาสในการใช้ของ
*   **API Security (สถาปัตยกรรมทางเลือกในอนาคต):** ด้วยความที่เป็นระบบ Client-Server หมายความว่าในอนาคตบริษัทสามารถว่าจ้างให้ทำ Application บน Mobile ของ iOS/Android ได้ทันที โดยดึงและชี้ข้อมูลมายังฐานเก็บตรงกัน โดยไม่ต้องรื้อโค้ดฝั่งหลังบ้าน (Backend) ทำใหม่แต่อย่างใด
